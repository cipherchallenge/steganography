# steganography
- Install Python, Pillow and Click.
- To hide `answers1.png` in `challenge1.png`, run `python steganography.py merge --image1=challenge1.jpg --image2=answers1.jpg --output=challege1-out.png`
- Write the functions `unmerge` in `steganography.py` to extract the hidden image.
